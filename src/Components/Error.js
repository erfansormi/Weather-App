import React from 'react'

const style = {
    padding: "5px 0px",
    backgroundColor: "#ececec",
    borderRadius: 8,
    minWidth: 200,
    width: "100%",
    maxWidth: 340,
    margin: "0 auto",
    textAlign: "center",
    textAlign: "center",
    textTransform: "capitalize",
    fontSize: "16px",
    color:"#ececec",
    backgroundColor: "#222",
    marginBottom: 15
}
const Error = () => {

    return (
        <div style={style}>
            city name sented invalid!
        </div>
    )
}

export default Error